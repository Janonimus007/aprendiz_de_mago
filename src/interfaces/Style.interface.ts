import { TextStyle, ViewStyle } from "react-native";

export interface IStyles {
    container: ViewStyle;
    containerItem: ViewStyle;
    textItem: TextStyle;
    textDelete: TextStyle;
    listContainer: ViewStyle;
  }